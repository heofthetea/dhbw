> [[Sortieren|Sortieren]] einer [[SQL select]] query.

```sql
SELECT...
ORDER BY attribute_name [ASC|DESC];
```



> [!hint] Kann nach der "n"-ten Spalte sortiert werden:
> ```sql
> select a, b, c
> from table 
> order by 2
> ```
> Sortiert nach b
