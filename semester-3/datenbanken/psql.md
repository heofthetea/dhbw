> [[Linux]] CLI für [[PostgreSQL]]

```bash
psql -h localhost -p 5432 -U postgres <database_name>
```

## Commands
- `\l` lists databases
- `\dt` listst table in current database
- `\c <name>` changes Database