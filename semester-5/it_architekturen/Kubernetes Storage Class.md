> A [[kubernetes]] Component to automatically manage Storage

- automatically manages [[Persistent Volume|Persistent Volumes]]
	- requests the storage from the storage backends

> [!hint] They're still referenced by a [[Persistent Volume Claim]] => the storage class simply creates the requested storage.