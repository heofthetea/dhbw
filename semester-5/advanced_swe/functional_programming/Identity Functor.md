> [[Functor]] mapping every [[Object]] and every [[Morphism]] to itself

[[Functor]], [[Identity Morphism]]

## Implementation
> [!hint] This is [[Isomorphism|isomorph]] to the naive `fmap f = f` - this is just for demonstration purposes that Functor actually _does_ something.
```rust
fmap f = (Identity x) -> (Identity f x)
```