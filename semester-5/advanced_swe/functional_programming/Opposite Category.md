> Reverse all [[Morphism|Morphisms]] of the original [[Category]]


$$C^{op} = (ob(C), \set{b \longrightarrow a | \exists_{m\in hom(C)}m: a \longrightarrow b})$$

> [!hint] The [[Initial Object]] is the Terminal Object of its reverse category.