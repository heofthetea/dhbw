[[nmap]]

- use [[nmap]] to gather information about the system
- use `db_import` to import an nmap scan, or use `db_nmap` as an [[nmap]] wrapper

```
msf> hosts # shows host information (ip addresses, os info etc)
msf> services # shows services that nmap has discovered through -sV
```

