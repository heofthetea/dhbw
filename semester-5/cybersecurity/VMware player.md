> Bullshit


## Installation
- insane fucking mess
- you can get to a "free" downloads page by googling
- -> downloads a `.bundle` (??) file, which is just a [[Bash script]]
	- -> that _extracts_ an installer to `/usr/bin/vmware-installer`

> [!hint] That installer can apparently be used to uninstall the fucking behemoth again

