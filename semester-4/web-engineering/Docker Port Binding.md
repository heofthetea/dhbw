```
<external-port>:<internal-port>
```

> [!hint] Uses [[NAT]] to forward the address into the local [[Docker Networking|Docker network]].


> [!warning] Port Bindings override the **EXPOSE** statement in a [[Dockerfile]]