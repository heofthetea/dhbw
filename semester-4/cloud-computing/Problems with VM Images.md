[[ISO image]]

- no standardization (there arae [[ISO image|ISO]], AWS images, ...)
- ISO images are large af (the träp debian image is 1G)
- different software versions for each image
- Images are fully deployed ([[Docker Image|docker images]] are incremental)

