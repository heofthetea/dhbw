usage in [[Master C]] and [[Hub Java]] 
```java
String myString = (condition) ? "condition is true" : "condition is false"
```

In python:
```python
mystring = "condition is true" if conditin else "condition is false"
```
