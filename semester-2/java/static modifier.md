Access to a Variable/Method or [[Code Block]] with the `static` modifier can (and has to lol) happen without instanciating the [[Java Class]]

> [!info] A static variable lives as long as the class lives.
