> A singleton [[Menge|Set]] -> Set with exactly one element.

$$\set{\varnothing}$$

> [!hint] The only reason I'm not using the conventional `()` for this is because it looks stupid in latex and there's no `\unit` command
## in [[Rust]]
> [!hint] somewhat equivalent to [[null]] - but not a Pointer. Just... "nothing", think [[Leere Menge]]

- the return value of a [[Function]] that has no return type

```rust
let x: () = ();
```